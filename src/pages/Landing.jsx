import Button from "../components/atoms/Button/Button";

const Landing = () => {
  return (
    <div className={``}>
      <Button color={"authBtn"} className={`w-full h-15`}>
        Salam
      </Button>
    </div>
  );
};

export default Landing;
