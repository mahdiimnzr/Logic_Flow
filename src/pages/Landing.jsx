import Button from "../components/atoms/Button/Button";

const Landing = () => {
  return (
    <div className={`bg-green-dark`}>
      <Button color={""} className={`w-full h-15`}>
        Salam
      </Button>
    </div>
  );
};

export default Landing;
